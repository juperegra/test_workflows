name: master


env:
    DAY_OF_WEEK: Monday

on:
    push:
        branches:
            - main


jobs:
    test:
        uses: ./.github/workflows/test.yml
        with:
            enviroment: ${{github.DAY_OF_WEEK}}